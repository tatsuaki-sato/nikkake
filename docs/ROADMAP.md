# ROADMAP

サーバ主導アーキテクチャ（Rails + GraphQL、Web/RN/Flutter/KMPの4クライアント）への
移行後、「終わった」と言える状態にするための残作業計画。

経緯: 一度「終わった」と報告したが、Dockerを一度も動かしていないなど
未検証の範囲を正確に報告できていなかった。そのため全件を洗い出し、
このファイルで管理する。**できていないことは隠さず、ここに残す。**

検証結果・不具合の記録は [QA.md](QA.md) を見ること。このファイルは
「何が終わっていて、何が残っていて、何をお願いしているか」の一元管理用。

---

## 全体像

| 領域 | 状態 |
|---|---|
| サーバ移行そのもの（Rails/GraphQL、4クライアント対応） | ✅ 完了 |
| A. 要件違反・機能欠落の是正 | ✅ 完了 |
| B. 動作検証（Docker / 本番 / 実機） | 🔶 進行中（本番デプロイまで完了） |
| C. セキュリティ・運用 | ✅ 完了（C3〜C5除く） |
| D. 開発基盤（CI等） | ✅ 完了 |
| E. 死んだコード・整合性 | ✅ 完了（E2, E3除く） |
| F. 未決定事項 | ❌ 未着手 |
| G. その他、途中で見つかった追加課題 | 🔶 進行中 |

---

## A. 要件違反・機能欠落 — ✅ 完了

| # | 内容 | 状態 |
|---|---|---|
| A1 | ネイティブ3実装のサインインをSupabase AuthからRailsへ | ✅ |
| A2 | 設定画面の文言をサーバ移行後の実態に合わせる | ✅ |
| A3 | オフライン時のルーティン保存ブロック | ✅ |
| A4 | 「最終同期」等、Supabase時代のUI除去 | ✅ |
| A5 | KMPのサーバ接続 | ✅ |

## B. 動作検証 — 🔶 進行中〜ブロック中

| # | 内容 | 状態 |
|---|---|---|
| B1 | Dockerを実際に動かして検証する | ✅ 完了（2026-08-10）。設定ミス4件発見・修正・再ビルドで動作確認・push・CI green まで完了。詳細は[QA.md](QA.md#docker-composeymlで見つけた不具合2026-08-10) |
| B2 | 本番へデプロイして常設URLを用意する | ✅ 完了（2026-08-22）。Render(Web Service) + Supabase(PostgreSQL, Session pooler)で稼働中。`https://nikkake-api.onrender.com` で匿名アカウント作成・認証つきクエリまで確認済み |
| B3 | 実サーバ・実機での動作確認 | 🔶 進行中 — APIの疎通は確認済み。クライアントからの接続確認はこれから |
| B4 | iOS/Android実機での確認 | ❌ 今回は着手しない（tatsuakiさんの手元でのみ可能） |
| B5 | 複数端末で同じアカウントを使う確認 | ❌ 今回は着手しない |

## C. セキュリティ・運用 — ✅ 完了（一部除く）

| # | 内容 | 状態 |
|---|---|---|
| C1 | レート制限（`createAnonymousAccount`） | ✅ |
| C2 | 匿名アカウントのGC | ✅ |
| C3 | WebのトークンをhttpOnly Cookie化 | ❌ 今回は着手しない |
| C4 | メール送信基盤の選定 | ❌ 今回は着手しない |
| C5 | PostgreSQL RLSを最終防壁として入れるか | ❌ 未決定・今回は着手しない |
| C6 | `config.hosts` を本番ホスト用に設定 | ✅ |

## D. 開発基盤 — ✅ 完了

| # | 内容 | 状態 |
|---|---|---|
| D1 | CI構築 | ✅ 全10ジョブgreen |
| D2 | 未pushコミットのpush | ✅ |
| D3 | Webの単体テスト | ❌ 今回は着手しない（E2Eのみ） |
| D4 | KMPのCompose UIテスト | ❌ 今回は着手しない（`compose.uiTest`がCompose 1.5.11で未解決） |

## E. 死んだコード・整合性 — ✅ 完了（一部除く）

| # | 内容 | 状態 |
|---|---|---|
| E1 | Supabase残骸の撤去（RN/Flutter/KMP） | ✅ |
| E2 | `changes(since:)` を使うか消すか | ❌ 未決定・今回は着手しない |
| E3 | WebのPWA化 | ❌ 今回は着手しない |

## F. 未決定事項

| # | 内容 | 状態 |
|---|---|---|
| F1 | 匿名データがある端末で既存アカウントにサインインしたときのマージ方針 | ❌ 未決定 |

## G. 途中で見つかった追加課題

| # | 内容 | 状態 |
|---|---|---|
| G1 | 3クライアントの `DEVELOPMENT_CONTEXT.md` がSupabase同期時代の内容のまま | ❌ 未着手。GraphQL/Rails/ServerRepository/BackendSwitchを反映した全面書き直しが必要 |
| G2 | 契約(`schema.graphql`)と実装の乖離検知が壊れている | ❌ 未着手。`rake graphql:dump`が`schema.generated.graphql`に書くのに、CIは`schema.graphql`をdiffしていて常にノーオペレーション。実際に`createAnonymousAccount`の引数形が契約と実装で食い違っていたが検知されなかった（クライアントは正しい形で呼んでいるため実害は無い）。詳細は[QA.md](QA.md) |

---

## tatsuakiさんにお願いしていること

私からは実行できないため、これが揃うまで該当項目は進みません。

| 項目 | 状態 |
|---|---|
| Docker Desktopのインストール | ✅ 完了（2026-08-10） |
| Renderのアカウント作成 | ✅ 完了（2026-08-22） |
| SupabaseのPostgres接続文字列（`DATABASE_URL`）の共有 | ✅ 完了（2026-08-22） |
| （B2後）実機（iOS/Android）での確認 | ❌ 未 |

---

## 次にやること（優先順位）

1. B3（実サーバ・実機での動作確認）— APIの疎通は確認済み。各クライアントから本番へ繋いで確認する
2. G2（契約とCIの乖離検知の復旧）— 実害は無いが、CIの信頼性に関わるため早めに直す
3. G1（開発者向けドキュメントの書き直し）
4. B4/B5（実機・複数端末）— tatsuakiさんの手元でのみ可能
