# QA

## 最新の実行結果

実行日: 2026-08-02 / 環境: macOS 15 (Apple Silicon), Node 22, Flutter 3.44.6 (Dart 3.12.2), JDK 17.0.19

### React Native

| 項目 | コマンド | 結果 |
|---|---|---|
| 型チェック | `npx tsc --noEmit` | ✅ エラー0 |
| 単体テスト | `npx jest` | ✅ **91 passed** / 6 suites |
| E2E | `npx playwright test` | ✅ **84 passed** (chromium 42 + Mobile Chrome 42) |

### Flutter

| 項目 | コマンド | 結果 |
|---|---|---|
| 静的解析 | `flutter analyze` | ✅ No issues found |
| 単体＋シナリオ | `flutter test` | ✅ **101 passed** |

### Kotlin Multiplatform

| 項目 | コマンド | 結果 |
|---|---|---|
| 単体テスト（desktop） | `./gradlew :shared:desktopTest` | ✅ **77 passed** / 0 failures |
| iOSコンパイル | `:shared:compileKotlinIosSimulatorArm64` | ✅ BUILD SUCCESSFUL |
| Androidコンパイル | `:shared:compileDebugKotlinAndroid` + `:androidApp:...` | ✅ BUILD SUCCESSFUL |
| Desktopコンパイル | `:desktopApp:compileKotlinJvm` | ✅ BUILD SUCCESSFUL |

**合計 353 テスト、全てグリーン。**

### この回で直した既存の不具合

| 内容 | 影響 |
|---|---|
| `nikkake_kmp/build.gradle.kts` に `plugins` ブロックが2つあった | **KMPが一切ビルドできない状態だった** |
| `desktopApp` に `desktopMain` と `jvmMain` の重複した `main.kt` | 死んだコードが残っていた |
| RNのPlaywrightに `webServer` 設定が無かった | 手動でdev serverを起動しないとE2Eが動かなかった |
| RNの `tsconfig.json` に jest の型が無かった | テストファイルで型チェックが落ちていた |

---

## 未検証の項目

正直に残しておきます。

| 項目 | 状態 | 理由 |
|---|---|---|
| iOS実機 / シミュレータでの動作 | ❌ 未実施 | 3実装ともコンパイルは通るが、実機での描画・操作は未確認 |
| Android実機 / エミュレータでの動作 | ❌ 未実施 | 同上 |
| **Supabaseとの実通信での同期** | ❌ 未実施 | 同期テストは全てモック。実DBに対する疎通・RLS・マイグレーション適用は未確認 |
| `20260801000000_local_first_sync.sql` の適用 | ❌ 未適用 | 実DBへの適用が必要 |
| KMPのCompose UIテスト | ❌ 未整備 | `compose.uiTest` が Compose 1.5.11 で解決できない |
| 複数端末間での同期の突き合わせ | ❌ 未実施 | 実アカウントが要る |

**リリース前に必ず実施すべきは、上記のうち「Supabaseとの実通信」です。**
同期ロジックはモックでしか検証されていません。

---

## リリース前チェックリスト

### 1. 静的チェックと自動テスト

- [ ] `cd nikkake_react_native && npm run verify`
- [ ] `cd nikkake_flutter && flutter analyze && flutter test`
- [ ] `cd nikkake_kmp && ./gradlew :shared:desktopTest`

### 2. Supabase

- [ ] `supabase/migrations/` の3ファイルを順に適用
- [ ] プリセット種目25件が固定IDで入っていることを確認
- [ ] RLSが有効で、他ユーザーの行が見えないことを確認

### 3. 手動確認（実装ごと・実機で）

**起動（最重要）**

- [ ] アプリを削除してから入れ直し、**サインインせずに**起動できる
- [ ] 起動直後のホームに「いつものルーティン」が出ている
- [ ] タップするとその場でワークアウトが始まる
- [ ] **どこにもログイン画面が出てこない**
- [ ] 機内モードでも上記が全て動く

**ルーティン**

- [ ] 作成 → ホームと一覧の両方に出る
- [ ] 名前空 / 種目0件 / 曜日未選択 でそれぞれエラーが出て保存されない
- [ ] 編集 → 内容が保存される。過去の記録が消えていない
- [ ] 停止 → ホームから消える。一覧では薄く「停止中」
- [ ] 削除 → 確認ダイアログが出る。記録は残る

**ワークアウト**

- [ ] セット完了で休憩タイマーが自動で始まる
- [ ] タイマーをタップでスキップできる
- [ ] 時間種目（プランク）で秒の入力欄になる
- [ ] セットの増減ができる。1本未満にはならない
- [ ] 完了 → サマリーが出て、連続記録が更新される
- [ ] 中断 → 何も保存されない
- [ ] 2回目に開始すると前回の値が初期値に入っている
- [ ] アプリを再起動しても記録が残っている

**進捗**

- [ ] 記録0件で空状態
- [ ] 1回記録すると集計・グラフ・カレンダーが出る
- [ ] 7日/30日の切り替えが効く
- [ ] 種目を切り替えると推移が変わる

**設定**

- [ ] 未サインインで「この端末にだけ保存中」と出る
- [ ] 件数が実際のデータと合っている
- [ ] データ初期化 → 確認後、初期状態に戻る

### 4. バックアップ（サインイン）

- [ ] ローカルで数件記録してからアカウント作成 → **その記録がクラウドへ上がる**
- [ ] 別端末で同じアカウントにサインイン → 記録が降りてくる
- [ ] 片方で編集 → もう片方に反映される
- [ ] 片方で削除 → もう片方でも消える（論理削除が伝播する）
- [ ] 機内モードで記録 → オンラインに戻すと同期される
- [ ] **サインアウトしてもローカルの記録が消えない**
- [ ] 別アカウントでサインインし直すと、そのアカウントのデータになる

### 5. 3実装のパリティ

- [ ] 同じ初期ルーティンが同じ内容で入っている
- [ ] バリデーションのメッセージが3つとも同じ
- [ ] 配色・余白が揃っている
- [ ] ストリークの数え方が同じ（同じ操作をして同じ日数になる）

---

## 既知の注意点

### KMPのビルドに必要なもの

- JDK 17（`JAVA_HOME` を設定）
- Androidターゲットをビルドするなら Android SDK（`local.properties` の `sdk.dir` か `ANDROID_HOME`）

`local.properties` は `.gitignore` 済みなので、クローン後に各自で作る必要があります。

### RNのE2Eを書くときの落とし穴

expo-router のWeb実装ではタブを切り替えても前の画面のDOMが残ります。
画面コンテナのtestIDでスコープを切らないと strict mode violation になります。
詳細は [TESTING.md](TESTING.md#e2eplaywright)。

### Flutterのウィジェットテストのビューポート

既定の 800×600 だとルーティン作成フォームが縦に収まらず、画面外のウィジェットは
ビルドされないためタップできません。`pumpApp` で 1200×4000 に広げています。

### Supabase anon キーがソースに直書きされている

3実装ともクライアントに埋め込んでいます。anon キーは公開前提の値で、
行単位のアクセス制御はRLSが担うので設計上は問題ありません。
ただし環境を分ける段階になったら、ビルド時の環境変数へ移すべきです。
